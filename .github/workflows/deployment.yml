name: "Server Deployment"

on:

  push:

      branches: [main]

jobs:

  deployment:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v2

    - name: Say Hello!

    run:
      echo "Hello there!"
      echo `whoami`
      echo $GITHUB_WORKSPACE
      echo `pwd`
      echo `ls`